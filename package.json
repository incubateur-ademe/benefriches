{
  "name": "benefriches-root",
  "license": "MIT",
  "engines": {
    "node": "24",
    "pnpm": "11.5.2"
  },
  "version": "1.0.0",
  "packageManager": "pnpm@11.5.2",
  "scripts": {
    "prepare": "husky || true",
    "lint": "pnpm run -r lint",
    "format": "pnpm run -r format",
    "test": "pnpm run -r test",
    "format:check": "pnpm run -r format:check",
    "typecheck": "pnpm run -r typecheck",
    "build": "pnpm run -r build",
    "agent-skills:sync": "node scripts/sync-agent-skills.mjs",
    "agent-skills:check": "node scripts/sync-agent-skills.mjs --check",
    "test:scripts": "node --test \"scripts/**/*.spec.mjs\" \".tooling/**/*.spec.mjs\""
  },
  "devDependencies": {
    "@upstash/context7-mcp": "4.0.7",
    "@vitest/coverage-v8": "^5.0.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.0",
    "oxlint": "^1.82.0",
    "oxlint-tsgolint": "^0.23.0",
    "prettier": "~3.9.6",
    "typescript": "~5.9.3",
    "vitest": "~5.0.0"
  }
}
