name: Deploy web on scalingo
on:
  push:
    branches:
      - "chore/init-scalingo"
jobs:
    deploy:
      name: "Build front and deploy to scalingo on ${{ vars.SCALINGO_APP_NAME }}"
      runs-on: ubuntu-latest
      environment: staging
      steps:
        - uses: actions/checkout@v3
        - uses: pnpm/action-setup@v2
          with:
            version: 8
        - name: Use Node.js
          uses: actions/setup-node@v3
          with:
            node-version: "18"
            cache: "pnpm"
        - name: Install dependencies
          run: pnpm install
        - name: Build web
          run: pnpm build
        - name: Create Archive
          run: mkdir -p scalingo/web && cp -r web/dist "$_" && cp -r web/scalingo/* "$_" && cp web/scalingo/.buildpacks "$_" && tar -czf web-scalingo.tar.gz scalingo
        - name: Archive dist
          uses: actions/upload-artifact@v3
          with:
            name: web-scalingo-v${{ github.run_number }}
            path: web-scalingo.tar.gz
        - name: Install scalingo CLI
          run: |
            wget -qO- https://cli-dl.scalingo.com/install.sh | bash
            echo "$HOME/bin" >> $GITHUB_PATH
        - name: Login to scalingo
          run: scalingo login --api-token ${{ secrets.SCALINGO_API_TOKEN }}
        - name: Download web artefact
          uses: actions/download-artifact@v3
          with:
            name: web-scalingo-v${{ github.run_number }}
        - name: Deploy front
          run: scalingo --app ${{ vars.SCALINGO_APP_NAME }} deploy web-scalingo.tar.gz
