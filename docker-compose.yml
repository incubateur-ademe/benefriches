version: "3"
services:
  web:
    build:
      context: app
    environment:
      - CHOKIDAR_USEPOLLING=true
      - NODE_ENV=development
    volumes:
      - ./app:/app
    ports:
      - 3000:3000
    stdin_open: true
    tty: true