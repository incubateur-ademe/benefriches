import type { InfoStepHandler } from "../stepHandler.type";

export const CreationResultHandler: InfoStepHandler = {
  stepId: "RENEWABLE_ENERGY_CREATION_RESULT",

  // Terminal step — no next step
};
