import { describe, it } from "vitest";

describe("Urban project buildings sequencing - reuse only with demolition", () => {
  it.todo("forward navigation includes demolition info and skips new-construction branch");
  it.todo("backward navigation returns through demolition info to footprint-to-reuse");
});
