import { describe, it } from "vitest";

describe("Urban project buildings sequencing - without buildings", () => {
  it.todo("forward navigation follows the no-buildings branch");
  it.todo("backward navigation returns through the no-buildings branch");
});
