import { describe, it } from "vitest";

describe("Urban project buildings sequencing - reuse and new construction", () => {
  it.todo("forward navigation includes existing uses and new-buildings uses steps");
  it.todo("backward navigation returns from new-buildings uses to chapter entry");
});
