import { describe, it } from "vitest";

describe("Urban project buildings sequencing - reuse only without demolition", () => {
  it.todo("forward navigation follows the reuse-only path without demolition info");
  it.todo("backward navigation returns through the reuse-only path without demolition info");
});
